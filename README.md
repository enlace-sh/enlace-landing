# ENLACE

## Landing page

Sitio web estático disponible en [`index.html`](index.html). Se sirve directamente desde cualquier servidor HTTP — no requiere build steps ni dependencias.

```sh
# Ejemplo con Python
python3 -m http.server
```

---
